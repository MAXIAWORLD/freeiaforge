services:
  backend:
    build: ./backend
    ports:
      - "8002:8002"
    env_file:
      - path: backend/.env
        required: false
    volumes:
      - freeai_data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  freeai_data:
